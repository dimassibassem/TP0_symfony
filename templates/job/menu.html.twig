<ul class="navbar-nav me-auto mb-2 mb-lg-0">
    {% for item in mymenu %}
        <li class="nav-item">
            <a class="nav-link" href="{{ item["route"] }}">{{ item["intitule"] }}</a>
        </li>
    {% endfor %}
</ul>
